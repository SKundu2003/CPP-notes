#include<iostream>

using namespace std;

int main()
{
//  normal operators
    int a, b= 2;
    // a == b; // "=="-> this operator copy the value of b in a
    // a>= b;  // ">=" -> 'a' is grater than or equal 'b' 
    // a<= b;  // "<=" -> 'a' is lasethan or equal to 'b'

//  logical operator 
    // a<10 && a>0;  //"&&" -> this operator is the AND operator. where both condictions have to be true 
    // a<10 || a<0;  //"||" -> this operator is the OR operator. where just any one condictions have to be true 
    return 0;
}